# 企业／项目权限管理联调记录

## 范围与协调

- 需求来源：`.spaces/01.企业级项目级/需求文档.md`。
- 验收映射：`artifacts/dev-team/PRD.md` 中的 AC-01～AC-15。
- 前后端基于 `artifacts/dev-team/contract.md` 和 `shared/types` 实现；契约变化由主控同步。
- 当前记录保留实施过程；最终 15 项验收已完成，详细证据见 [测试报告](./test-report.md)。

## 基线与环境

- `vp env doctor` 通过；Vite+ 管理的 Node 24.21.0 可用。PATH 中的 mise shim 未做系统级调整。
- `vp install --frozen-lockfile --offline` 因非交互环境要求确认重建依赖目录而退出；未删除现有依赖。现有依赖可以运行测试。
- 沙箱内 Vite／Chrome 启动子进程遇到 `spawn EPERM`；经执行工具批准，在沙箱外执行正常。
- `vp run --filter web test`：原有 3 个测试文件、29 项测试全部通过。
- `apps/server` 目录直接执行 `vp test`，运行期开启 `RUN_QUEUE_INTEGRATION=true` 并指定独立 Redis：4 个测试文件、14 项测试全部通过。`vp run` 会过滤该环境变量，其跳过结果不计入真实集成验证。
- 全仓 `vp check` 基线失败：176 个原有文件存在格式问题；`vp check --no-fmt` 基线存在 `packages/i18n/core/src/i18n.ts` 的两处 `console` 类型错误及 3 项警告。实施不会批量改写无关文件，交付区分原有问题与新增问题。
- Playwright 使用已有运行时包及已安装 Chrome，启动、渲染和读取页面的烟测通过；没有安装新浏览器或替换项目依赖。

## 数据隔离

- 本次验收独立创建临时 PostgreSQL 18 和 Redis 8 容器，仅监听 `127.0.0.1`，端口分别为 15432 和 16379。
- 容器名称为 `codex-enterprise-01a085-pg` 与 `codex-enterprise-01a085-redis`，数据库为 `enterprise_test`。
- 测试 API 实际监听 `http://127.0.0.1:3301`，Web 监听 `http://127.0.0.1:5301`，通过 `/api` 代理到测试 API；原有开发数据库、Redis 及其数据不修改。
- 独立 MinIO 容器 `codex-enterprise-01a085-minio` 监听本机 19000，使用单独测试 bucket 验证原有上传，不改现有对象存储。
- 测试数据库密码与会话密钥运行期随机生成，仅在系统临时配置中传递，不写入仓库或报告。测试账号亦按运行期生成。
- 正式交付需提供迁移和初始化说明；测试环境初始化成功不代替现有库兼容验证。

测试启动器为 `scripts/with-enterprise-test-env.mjs`，参数依次为临时 JSON 配置绝对路径、工程内工作目录、可执行文件及其参数。它检查测试数据库名称与本机地址，将配置仅注入子进程，不打印密钥。真实集成测试需直接调用包目录的测试命令，避免任务编排器过滤环境变量。

## 联调及缺陷

前后端已实现全部管理页面及契约接口。联调阶段发现的问题按所属模块回派修复，最终验收状态以 [测试报告](./test-report.md) 为准。

| 轮次                 | 发现与修复                                                                                                                                                          | 验证证据                                                                      |
| -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| 1 契约与代码评审     | 补齐项目管理员列表接口，管理员更换同步成员版本；完善按作用域的管理员保护、锁内重新鉴权和登录用户行锁；修正初始化脚本与构建产物启动路径                              | Shared 7/7、前后端构建；后端真实数据库集成测试                                |
| 2 首次真实启动       | Redis 会话校验 Lua 缺少局部 `isValidDigest`，撤销 Lua 大写 `DO` 导致执行失败；均修复并覆盖真实 Redis                                                                | 后端数据库及 Redis 集成首轮 12/12，无跳过；重新构建启动后 HTTP 受保护接口恢复 |
| 3 页面恢复与连续编辑 | 叶子页面 403 导致父布局丢失导航，首页停用后无法从工作空间入口恢复；改为独立父级作用域 loader 和已授权页面落点。资料保存同步响应中的最新版本，避免第二次编辑误报 409 | Web 43/43；31 个文件定向检查及完整 TypeScript/Vite 构建通过；浏览器继续复测   |
| 4 接口回归           | 停用最后有效平台管理员虽被拒绝，却提前撤销会话；改为先完成事务内业务约束、审计与结果读取，再在用户行锁内撤会话。原上传 session DTO 误用分片参数 schema，已改正      | 新增拒绝停用及审计失败保留会话断言；HTTP、浏览器与上传流程继续复测            |

测试脚本中客户端传入的 request ID 与服务端生成的 `x-request-id` 不同，审计检查已改用实际响应头。浏览器与接口并行测试分别使用已有的 Web／Mobile 会话类别，避免同终端单会话策略导致互相替换。

最终复测完成：Shared 7/7、Web 43/43、Server 33/33；独立 QA 的 HTTP 17/17、浏览器 13/13、旧库迁移 3/3、旧库 HTTP 2/2，合计 35 组通过。AC-12 另补初始化后引入新功能目录的真实数据库升级测试，确认只扩展对应内置管理员权限，其他角色、用户分配和菜单定制保持。AC-13 和 AC-14 的版本兼容及清理验证使用受控夹具，其边界在最终报告中单独注明。

## 实际启动与迁移

本次已在空的隔离数据库执行 `drizzle-kit migrate`，并完成服务启动、公开注册及首位平台管理员 CLI 初始化。旧库迁移另外使用 `enterprise_legacy_*` 数据库重放历史迁移，保留旧账号及上传 fixture 后升级，未访问开发库。

根目录的隔离后端启动命令为：

```powershell
node scripts/with-enterprise-test-env.mjs "$env:TEMP/codex-enterprise-01a085-runtime.json" apps/server node dist/src/main.js
```

迁移同样通过该启动器注入运行期环境，子命令为 `vp exec drizzle-kit migrate`；Windows 实际执行时指定了 `vp.exe` 的绝对路径以避免 shell 包装。通用构建、配置与首次使用命令见 [运行说明](./operations.md)。临时配置不属于交付配置，不应直接用于开发或正式部署。

## 验收环境清理

2026-09-10 已确认 3301、5301、3302 无测试进程监听，随后移除本次专属 PostgreSQL、Redis、MinIO 容器及匿名测试数据卷，并删除系统临时目录中的 4 份测试运行配置与身份凭据。保留测试脚本、脱敏 JSON 证据和截图；未改写开发环境配置、未迁移开发库、未提交 Git 或部署。文中的隔离端口与临时路径用于记录本次执行，当前不再提供运行中的测试预览；复现时需重新准备隔离运行配置。
