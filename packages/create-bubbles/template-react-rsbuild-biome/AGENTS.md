## 目录说明

- 修改文件前，须从文件所在目录逐级向上查找并阅读相关的 `AGENTS.md`（如存在），并遵循其中的说明。

## 按需规则

根据当前任务匹配下表，在执行相关操作前读取对应规则。不要预先加载整个 `.agents/rules/` 目录；任务范围变化时补读新命中的规则。

| 适用条件             | 规则文件                             |
| -------------------- | ------------------------------------ |
| 新增、修改或使用 SVG | [SVG 使用](.agents/rules/SVG使用.md) |
